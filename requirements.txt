# ══════════════════════════════════════════════════════════════════════════════
#  Sales LLM — Requirements
#  Install with:  pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════════════════════

# Core ML / Training
torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.29.0

# Data handling
pandas>=2.0.0

# Pickle is part of Python stdlib — no install needed
# argparse, textwrap, time, os, sys — all stdlib
